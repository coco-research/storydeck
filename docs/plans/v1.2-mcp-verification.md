# StoryDeck v1.2 MCP Plan — Verification Report

**Status:** VERIFY COMPLETE  
**Date:** 2026-07-22  
**Verifier:** /team verify  
**Subject:** `docs/plans/v1.2-mcp-integration.md` (plan-only — no implementation yet)

---

## Verdict

### **PASS (plan quality)** — ready for implementation

The plan is **technically feasible**, **aligned with user requirements**, and **correct about the v1.1 update model**. No CRITICAL gaps. Two MAJOR items to resolve during implementation (asar unpack, installer path detection).

**Implementation verdict:** N/A — no v1.2 code shipped yet. Baseline codebase tests green (see Evidence).

---

## Requirements Traceability Matrix

| ID | Requirement (from conversation) | Plan coverage | Status | Evidence |
|----|----------------------------------|---------------|--------|----------|
| R1 | MCP integration for installed StoryDeck | Architecture + 9 MCP tools + installer | **MET** | Plan §Architecture, §MCP Tool Specification, Phase 2–3 |
| R2 | v1.2 as MCP release | Version bump Phase 0, release Phase 4 | **MET** | Plan §Executive Summary, Phase 0/4 |
| R3 | Clarify auto-update vs reinstall | Update model table + Appendix B FAQ | **MET** | Plan §Executive Summary, Appendix B; confirmed against `src/updater.js` L1–7, `main.js` L121–134 |
| R4 | Cursor can read/write live board | Tools map to existing REST API | **MET** | Plan tool table; `src/server.js` L107–246 |
| R5 | Works with app running on system | Port discovery + runtime.json | **MET** | Plan §Port discovery; `main.js` L46–65 fallback listen |
| R6 | Local-first / no cloud | Non-goals + security section | **MET** | Plan §Non-Goals NG2, §Security |
| R7 | Data preserved on reinstall | Rollout plan states userData separate | **MET** | `main.js` L34–44, `tools/update-app.sh` L5–6 |
| R8 | Measurable success criteria | M1–M5 metrics table | **MET** | Plan §Success Metrics |

**Coverage:** 8/8 requirements **MET** at plan level.

---

## Technical Feasibility Audit

| Claim in plan | Codebase check | Result |
|---------------|----------------|--------|
| API sufficient for MCP (no new endpoints) | `handleApi` exposes state, CRUD, comments, export | **Confirmed** |
| Port may differ from 4321 | `listenWithFallback` uses port 0 on EADDRINUSE | **Confirmed** (`main.js` L48–65) |
| Hot updates cover `src/` only | `ALLOWED_PREFIXES = ['web/', 'src/']` in updater | **Confirmed** (`src/updater.js` L32) |
| Binary changes need reinstall | No `electron-updater` in deps | **Confirmed** (`package.json`) |
| MCP can live in `src/mcp/` | Updater allows `src/*` paths | **Confirmed** |
| Content mirror publish path | `tools/push-updates.sh` → `storydeck-content` | **Confirmed** |
| CI test gate | `release.yml` runs `npm test` | **Confirmed** |

---

## Plan Quality Review (Layer 3)

### CRITICAL — 0 findings

### MAJOR — 2 findings

| # | Finding | Recommendation |
|---|---------|----------------|
| M1 | Plan does not explicitly add `asarUnpack` for `src/mcp/**` in `package.json` build config | Add task P0.4: `"asarUnpack": ["src/mcp/**"]` in electron-builder config before Phase 4 |
| M2 | Installed app userData path is `storydeck` (lowercase) per live system, but README says `StoryDeck` in places | Installer must use `app.getPath('userData')` equivalent path; document actual path `~/Library/Application Support/storydeck/` |

### MINOR — 3 findings

| # | Finding | Recommendation |
|---|---------|----------------|
| m1 | `storydeck_list` client-side filter duplicates logic in `web/index.html` | Acceptable for v1.2; extract shared filter helper in v1.2.1 if needed |
| m2 | No explicit test for MCP when `runtime.json` missing but port 4321 works | Add fallback test in Phase 2 |
| m3 | Open Q3 (asar unpack) should be closed before Phase 0 ends | Mark decision: unpack `src/mcp/` |

### SUGGESTIONS — 2

| # | Suggestion |
|---|------------|
| s1 | Add `storydeck_health` as alias for `storydeck_status` — agents often search for "health" |
| s2 | Phase 2 dogfood: document `STORYDECK_URL=http://127.0.0.1:4321 node src/mcp/server.js` for dev before binary |

---

## Dependency & Sequencing Check

| Check | Result |
|-------|--------|
| Phases have clear depends-on chain | **Pass** |
| Binary vs hot-update split correct | **Pass** |
| Critical path identified | **Pass** (Phase 1 blocks reliable MCP) |
| Estimates realistic (~3 days) | **Pass** for scoped v1.2 |
| Destructive ops appropriately deferred | **Pass** (no import/reset in v1.2) |

---

## Update Model Verification (user question)

Verified against source:

| Layer | Auto-update? | Mechanism | Verified |
|-------|--------------|-----------|----------|
| Content (`web/`, `src/`) | **Yes**, next launch | `downloadUpdate()` in `main.js` L121–134, `src/updater.js` | **Yes** |
| App binary | **No** | Manual `.dmg` / `update-app.sh` | **Yes** |
| User data (`todo.db`) | N/A (persists) | `userData` path in `main.js` | **Yes** |

**Live system check (2026-07-22):** Installed v1.1.0, no `content-overlay/` — running bundled content. Hot-update channel exists but user is on bundled baseline.

---

## Evidence — Baseline Test Gate

```
Command:  cd /Users/Rijul_Kalra/ToDo && npm test
Exit:     0
Summary:  133 pass, 0 fail, 0 skipped
Duration: ~287ms
```

Plan does not break existing code (plan-only). Baseline green for implementation start.

---

## Gap List (implementation still needed)

| Priority | Gap | Phase |
|----------|-----|-------|
| P0 | No `src/mcp/` code exists yet | Phase 2 |
| P0 | No `runtime.json` write in `main.js` | Phase 1 |
| P1 | No `@modelcontextprotocol/sdk` dependency | Phase 0 |
| P1 | No `tools/install-mcp.sh` | Phase 3 |
| P2 | No `asarUnpack` in build config | Phase 0 (add per M1) |

---

## Recommendation

| Action | Decision |
|--------|----------|
| **Proceed to implementation?** | **Yes** — plan verified |
| **Ship plan as-is?** | Yes, with M1/M2 folded into Phase 0 |
| **Blockers** | None |

---

## Feedback Entries (for team:feedback.md)

### 2026-07-22 | /team plan | StoryDeck v1.2 MCP

- **Tool reviewed:** Manual plan synthesis (no GSD)
- **Reviewer role:** domain-accuracy / technical-analyst
- **Finding:** StoryDeck userData folder is lowercase `storydeck` on macOS, easy to mismatch in docs/installer
- **Recommendation:** Installer resolves path dynamically; never hardcode `StoryDeck` vs `storydeck`
- **Status:** pending
- **Impact:** medium

### 2026-07-22 | /team verify | StoryDeck v1.2 MCP plan

- **Tool reviewed:** /team verify plan-only mode
- **Reviewer role:** qa-test-architect
- **Finding:** MCP server inside asar will fail unless unpacked — plan mentions in risks but not Phase 0 tasks
- **Recommendation:** Add explicit P0.4 `asarUnpack` task before release
- **Status:** pending
- **Impact:** high
