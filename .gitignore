# secrets / local env
.env
.env.local
# first-run API key store (lives beside the DB / in userData; never committed)
ai-config.json
**/ai-config.json

# dependencies
node_modules/

# packaged desktop build output
dist/

# ── PRIVATE OVERLAY ─────────────────────────────────────────────
# The McKinsey / first-user instance. Real seed, live DB, backups, and
# secrets live here and must NEVER be pushed. The public repo ships only
# the fictional data/seed.sample.json.
private/

# internal build/planning scaffolding — local only, not part of the public product
.team-ship/

# local data & artifacts (public build creates these at runtime)
data/*.db
data/*.db-*
backups/
data/seed.json
.ai-scratch/
screenshots/

# OS
.DS_Store
